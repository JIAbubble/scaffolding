# 工具类总览

本项目提供了8个常用的工具类，涵盖了日期时间、字符串、JSON、加密、验证、分页、Bean转换和文件操作等功能。

## 📚 工具类列表

| 工具类 | 说明 | 文档链接 |
|--------|------|----------|
| **DateUtil** | 日期时间工具类 | [DateUtil使用说明.md](./DateUtil使用说明.md) |
| **StringUtil** | 字符串工具类 | [StringUtil使用说明.md](./StringUtil使用说明.md) |
| **JsonUtil** | JSON工具类 | [JsonUtil使用说明.md](./JsonUtil使用说明.md) |
| **SecurityUtil** | 加密工具类 | [SecurityUtil使用说明.md](./SecurityUtil使用说明.md) |
| **ValidationUtil** | 验证工具类 | [ValidationUtil使用说明.md](./ValidationUtil使用说明.md) |
| **PageUtil** | 分页工具类 | [PageUtil使用说明.md](./PageUtil使用说明.md) |
| **BeanUtil** | Bean工具类 | [BeanUtil使用说明.md](./BeanUtil使用说明.md) |
| **FileUtil** | 文件工具类 | [FileUtil使用说明.md](./FileUtil使用说明.md) |

## 🚀 快速开始

### 1. DateUtil - 日期时间处理

```java
// 获取当前时间
String now = DateUtil.now();  // "2025-01-22 14:30:45"

// 格式化日期
String formatted = DateUtil.format(LocalDateTime.now());

// 日期计算
long days = DateUtil.daysBetween(startDate, endDate);
```

### 2. StringUtil - 字符串处理

```java
// 空值判断
boolean blank = StringUtil.isBlank(str);

// 命名转换
String camel = StringUtil.underscoreToCamel("user_name");  // "userName"

// 敏感信息脱敏
String masked = StringUtil.maskPhone("13812345678");  // "138****5678"
```

### 3. JsonUtil - JSON处理

```java
// 对象转JSON
String json = JsonUtil.toJsonString(user);

// JSON转对象
User user = JsonUtil.parseObject(json, User.class);

// 验证JSON
boolean valid = JsonUtil.isValidJson(json);
```

### 4. SecurityUtil - 加密工具

```java
// MD5加密
String md5 = SecurityUtil.md5("password");

// 密码加盐加密
String salt = SecurityUtil.generateRandomString(16);
String hashed = SecurityUtil.hashPassword("password", salt);

// 验证密码
boolean valid = SecurityUtil.verifyPassword("password", salt, hashed);
```

### 5. ValidationUtil - 数据验证

```java
// 邮箱验证
boolean valid = ValidationUtil.isEmail("test@example.com");

// 手机号验证
boolean valid = ValidationUtil.isPhone("13812345678");

// 密码强度验证
boolean strong = ValidationUtil.isStrongPassword("Pass123");
```

### 6. PageUtil - 分页工具

```java
// 创建分页对象
Page<User> page = PageUtil.createPage(1, 10);

// 分页结果转换
PageResult<User> result = PageUtil.toPageResult(iPage);
```

### 7. BeanUtil - Bean转换

```java
// 对象属性复制
BeanUtil.copyProperties(source, target);

// 对象类型转换
UserDTO dto = BeanUtil.convert(user, UserDTO.class);

// List转换
List<UserDTO> dtos = BeanUtil.convertList(users, UserDTO.class);
```

### 8. FileUtil - 文件操作

```java
// 读取文件
String content = FileUtil.readFile("path/to/file.txt");

// 写入文件
FileUtil.writeFile("path/to/file.txt", "content");

// 文件信息
long size = FileUtil.getFileSize("path/to/file.txt");
String ext = FileUtil.getExtension("file.pdf");
```

## 💡 使用场景

### 场景1：用户注册登录

```java
// 1. 验证用户输入
if (!ValidationUtil.isEmail(form.getEmail())) {
    return ResponseResult.fail("邮箱格式不正确");
}

// 2. 加密密码
String salt = SecurityUtil.generateRandomString(16);
String hashedPassword = SecurityUtil.hashPassword(form.getPassword(), salt);

// 3. 保存用户
User user = BeanUtil.convert(form, User.class);
user.setPassword(hashedPassword);
user.setSalt(salt);
userService.save(user);
```

### 场景2：API响应处理

```java
// 1. 查询数据
List<User> users = userService.list();

// 2. 转换为DTO
List<UserDTO> dtos = BeanUtil.convertList(users, UserDTO.class);

// 3. 脱敏处理
dtos.forEach(dto -> {
    dto.setPhone(StringUtil.maskPhone(dto.getPhone()));
});

// 4. 返回JSON
String json = JsonUtil.toJsonString(ResponseResult.success(dtos));
```

### 场景3：分页查询

```java
// 1. 创建分页对象
Page<User> page = PageUtil.createPage(current, size);

// 2. 执行查询
IPage<User> iPage = userService.page(page);

// 3. 转换为DTO
PageResult<UserDTO> result = PageUtil.convert(iPage, user -> {
    return BeanUtil.convert(user, UserDTO.class);
});

// 4. 返回结果
return ResponseResult.success(result);
```

## 📖 详细文档

每个工具类都有详细的使用说明文档，包含：

- ✅ 方法说明
- ✅ 参数说明
- ✅ 返回值说明
- ✅ 使用示例
- ✅ 实际应用场景
- ✅ 注意事项

请查看对应的文档获取更多信息。

## 🔗 相关资源

- [项目README](../README.md) - 项目总体说明
- [SecurityUtil使用说明](./SecurityUtil使用说明.md) - 密码加密详细说明

## ⚠️ 注意事项

1. **空值安全**：所有工具类方法都进行了空值检查
2. **性能考虑**：大量数据操作时注意性能优化
3. **异常处理**：文件操作等方法可能抛出异常，建议使用 try-catch
4. **线程安全**：工具类方法都是静态方法，线程安全

---

**祝使用愉快！** 🎉

